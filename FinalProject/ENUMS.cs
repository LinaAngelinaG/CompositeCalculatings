
enum Method
{
    map,
    reduce,
    zip,
    unzip
}

public enum Map
{
    add,
    multiply,
    divide,
    exponentiate
}
public enum Reduce
{
    max,
    min,
    overage,
    sum,
    length
}
public enum Zip
{

}
